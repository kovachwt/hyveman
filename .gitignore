# ── .NET build artifacts ───────────────────────────────────────────────────
bin/
obj/
# agent build.ps1 output (out\hyveman-agent.exe)
out/
publish/
# local dotnet publish output (e.g. run/api/)
run/
*.nupkg

# ── Test results & coverage ────────────────────────────────────────────────
TestResults/
*.trx
*.coverage
*.coveragexml
coverage.*
*.opencover.xml

# ── Visual Studio / Rider / VS Code ────────────────────────────────────────
.vs/
.idea/
.vscode/
*.user
*.suo
*.userosscache
*.sln.docstates
*.ncrunch*
*_i.c
*_p.c
*.njsproj
*.sln.ide

# ── Logs & temp files ──────────────────────────────────────────────────────
*.log
*.tmp
*.temp
*.bak
~$*
*~

# ── OS noise ───────────────────────────────────────────────────────────────
Thumbs.db
Desktop.ini
.DS_Store

# ── Local server data dir (never commit) ───────────────────────────────────
# The repo-local API data dir (SQLite DB, config.json, the AES-GCM server
# key K — losing it loses all vault secrets — backup/, logs/):
/data/

# ── Dev-machine scratch (tokens, certs, DBs — never commit) ───────────────
devdata/
